# Related producer content

Reviewed packages live at `<country>/<producer_id>.json`. The area CSV remains
the producer registry. See `docs/PRODUCER_CONTENT.md` for the format and local
review commands. Empty directories do not register producers or public routes.
