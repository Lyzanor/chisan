{
  "framework": "nextjs",
  "regions": ["fra1"],
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm check:translations && pnpm db:assert-current && pnpm build",
  "ignoreCommand": "node scripts/vercel-ignore-build.mjs"
}
