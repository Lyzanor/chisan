name: verify

# Runs the repository gate on every push to main and every pull request.
# CI always runs `verify:ai`, the superset: the data/code split in AGENTS.md
# exists to save an agent's time locally, not a runner's.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - run: npm install --global pnpm@11

      - run: pnpm install --frozen-lockfile

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Best effort: without these, test:enrich-images skips itself and says so
      # in the log, exactly as it does on a machine without image tooling.
      - name: Install image tooling
        continue-on-error: true
        run: python -m pip install -r scripts/requirements-image-tools.txt

      - run: pnpm verify:ai
